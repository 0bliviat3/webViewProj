<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#121212">

    <!-- 상단 메뉴바 -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:background="#1E1E1E"
        android:gravity="center_vertical"
        android:paddingHorizontal="8dp">

        <TextView
            android:id="@+id/titleText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:text="Console WebView"
            android:gravity="center" />

        <ImageButton
            android:id="@+id/btnMenu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_more"
            app:tint="#FFFFFF" />
    </LinearLayout>

    <!-- 웹뷰 -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000" />

    <!-- 콘솔 영역 (기본 숨김) -->
    <LinearLayout
        android:id="@+id/consoleContainer"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:orientation="vertical"
        android:visibility="gone"
        android:background="#121212">

        <ScrollView
            android:id="@+id/scrollConsole"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#121212">

            <!-- 기존 TextView 대신 동적 콘솔 블록을 담는 영역 -->
            <LinearLayout
                android:id="@+id/consoleOutputContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp" />
<!--            <TextView-->
<!--                android:id="@+id/consoleView"-->
<!--                android:layout_width="match_parent"-->
<!--                android:layout_height="wrap_content"-->
<!--                android:padding="8dp"-->
<!--                android:textSize="14sp"-->
<!--                android:fontFamily="monospace"-->
<!--                android:textColor="#DDDDDD"-->
<!--                android:text="-&#45;&#45; JS Terminal -&#45;&#45;\n" />-->
        </ScrollView>

        <!-- 콘솔 입력 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="#000000"
            android:padding="6dp">

            <TextView
                android:id="@+id/promptSymbol"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="> "
                android:textColor="#00FF88"
                android:textSize="14sp"
                android:fontFamily="monospace" />

            <EditText
                android:id="@+id/consoleInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Type JavaScript and press Enter"
                android:textColor="#FFFFFF"
                android:textColorHint="#666666"
                android:background="@android:color/transparent"
                android:padding="0dp"
                android:inputType="text"
                android:imeOptions="actionDone"
                android:fontFamily="monospace" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
